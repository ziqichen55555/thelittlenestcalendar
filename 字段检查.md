# ✅ 字段对应关系检查

## 📋 当前字段映射

### 应用内部 → Google Sheets（写入时）

| 应用字段 | Google Sheets 字段 | 状态 |
|---------|------------------|------|
| `booking.startDate` | `StartDate` | ✅ 正确 |
| `booking.endDate` | `EndDate` | ✅ 正确 |
| `booking.guests` | `GuestsNo` | ✅ 正确 |
| `booking.note` | `Note` | ✅ 正确 |
| `booking.color` | `Color` | ✅ 正确 |
| `booking.id` | ❌ 不发送 | ⚠️ 注意：你的脚本不接收 ID |

### Google Sheets → 应用内部（读取时）

| Google Sheets 字段 | 应用字段 | 状态 |
|------------------|---------|------|
| `ID` 或 `id` | `booking.id` | ✅ 正确（如果没有，使用行号） |
| `StartDate` 或 `startDate` | `booking.startDate` | ✅ 正确 |
| `EndDate` 或 `endDate` | `booking.endDate` | ✅ 正确 |
| `GuestsNo` 或 `Guests` 或 `guests` | `booking.guests` | ✅ 正确（兼容多种格式） |
| `Note` 或 `note` | `booking.note` | ✅ 正确 |
| `Color` 或 `color` | `booking.color` | ✅ 正确 |

## ⚠️ 发现的问题

### 1. ID 字段处理

**你的 Google Apps Script doPost 代码：**
```javascript
sheet.appendRow([
  data.StartDate || "",
  data.EndDate || "",
  data.GuestsNo || "",
  data.Note || "",
  data.Color || ""
]);
```

**问题**：没有保存 ID 字段！

**影响**：
- 添加预订时，ID 不会保存到 Google Sheets
- 更新和删除时，无法通过 ID 找到记录

**解决方案**：
1. 更新 Google Apps Script，在 appendRow 时包含 ID
2. 或者使用行号作为 ID（当前代码已处理）

### 2. 表名不一致

**你的代码：**
- `doPost` 使用：`"thelittlenestbookings"`
- `doGet` 使用：`"Bookings"`

**问题**：两个函数使用不同的表名！

**解决方案**：确保两个函数使用相同的表名。

## ✅ 当前代码的处理

代码已经处理了这些问题：

1. **ID 处理**：如果没有 ID，使用行号 `row-${index + 1}`
2. **字段映射**：自动处理大小写和多种字段名格式
3. **兼容性**：支持 `GuestsNo` 和 `Guests` 两种格式

## 🧪 测试建议

1. **添加一个测试预订**
2. **检查 Google Sheets**：
   - 表头应该是：`ID | StartDate | EndDate | GuestsNo | Note | Color`
   - 数据行应该包含所有字段
3. **检查应用**：
   - 预订应该正确显示
   - 日期、人数、备注都应该正确

## 📝 推荐的 Google Sheet 表头

确保第一行是：

```
ID | StartDate | EndDate | GuestsNo | Note | Color
```

## 🔧 如果发现数据不对

1. 检查 Google Sheet 的表头是否正确
2. 检查 Google Apps Script 的表名是否一致
3. 查看浏览器控制台的日志输出


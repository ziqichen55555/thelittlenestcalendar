# 🚀 Firebase 快速配置指南（5分钟完成）

## 为什么需要配置？

目前 Firebase 配置是占位符，数据无法保存到云端。配置后，数据会保存在云端，所有设备都能看到。

## 📝 配置步骤（非常简单）

### 第 1 步：创建 Firebase 项目（2分钟）

1. 访问：https://console.firebase.google.com/
2. 点击 **"添加项目"** 或 **"Create a project"**
3. 输入项目名称：`thelittlenestcalendar`（或任何你喜欢的名字）
4. 点击 **"继续"** → **"继续"** → **"创建项目"**
5. 等待创建完成（约 30 秒）

### 第 2 步：启用 Firestore 数据库（1分钟）

1. 在 Firebase Console 左侧菜单，点击 **"Firestore Database"**
2. 点击 **"创建数据库"**
3. 选择 **"以测试模式启动"**（开发阶段，完全免费）
4. 选择数据库位置：选择离你最近的区域（例如：`asia-southeast1` 或 `us-central1`）
5. 点击 **"启用"**

### 第 3 步：获取配置信息（1分钟）

1. 在 Firebase Console 左侧菜单，点击 **"项目设置"**（齿轮图标 ⚙️）
2. 向下滚动到 **"你的应用"** 部分
3. 点击 **"</>"** 图标（Web 应用）
4. 输入应用昵称：`thelittlenestcalendar-web`（或任何名字）
5. **不要**勾选 "Firebase Hosting"
6. 点击 **"注册应用"**
7. 你会看到配置代码，类似这样：

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456"
};
```

### 第 4 步：更新配置文件（1分钟）

1. 打开项目中的文件：`src/config/firebase.ts`
2. 找到这段代码：

```typescript
const firebaseConfig = {
  apiKey: "AIzaSyDummyKeyReplaceWithYourOwn",
  authDomain: "thelittlenestcalendar.firebaseapp.com",
  projectId: "thelittlenestcalendar",
  storageBucket: "thelittlenestcalendar.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456"
};
```

3. 将第 3 步复制的配置信息替换进去
4. 保存文件

### 第 5 步：设置安全规则（1分钟）

1. 在 Firebase Console，点击 **"Firestore Database"**
2. 切换到 **"规则"** 标签
3. 将规则改为：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /bookings/{document=**} {
      allow read, write: if true;
    }
  }
}
```

4. 点击 **"发布"**

### 第 6 步：测试（30秒）

1. 运行 `npm run build`
2. 打开浏览器控制台（F12）
3. 应该看到：`✓ Firebase 已配置并初始化`
4. 添加一个测试预订，应该看到：`✓ 预订已添加并保存到云端！`

## ✅ 完成！

配置完成后，所有数据都会自动保存到云端，在任何设备上都能看到！

## ❓ 遇到问题？

- **找不到 "Firestore Database"**：确保项目已创建完成
- **配置错误**：检查是否复制了完整的配置信息
- **权限错误**：确保安全规则已发布
- **还是看不到数据**：检查浏览器控制台的错误信息

## 💡 提示

- Firebase 免费版对个人使用完全足够
- 数据会自动同步，无需手动刷新
- 如果网络断开，应用会显示错误提示


# Firebase 云端存储配置说明

## 📋 概述

应用现在使用 Firebase Firestore 进行云端存储，数据会自动同步到所有设备。

## 🔧 配置步骤

### 1. 创建 Firebase 项目

1. 访问 [Firebase Console](https://console.firebase.google.com/)
2. 点击 "添加项目" 或 "Create a project"
3. 输入项目名称：`thelittlenestcalendar`（或你喜欢的名称）
4. 按照提示完成项目创建

### 2. 启用 Firestore 数据库

1. 在 Firebase Console 中，点击左侧菜单的 "Firestore Database"
2. 点击 "创建数据库"
3. 选择 "以测试模式启动"（开发阶段）
4. 选择数据库位置（建议选择离你最近的区域）
5. 点击 "启用"

### 3. 获取 Firebase 配置

1. 在 Firebase Console 中，点击左侧菜单的 "项目设置"（齿轮图标）
2. 向下滚动到 "你的应用" 部分
3. 点击 "</>" 图标（Web 应用）
4. 输入应用昵称（例如：`thelittlenestcalendar-web`）
5. 复制显示的配置信息

### 4. 更新配置文件

打开 `src/config/firebase.ts`，将配置替换为你从 Firebase Console 复制的配置：

```typescript
const firebaseConfig = {
  apiKey: "你的-apiKey",
  authDomain: "你的项目.firebaseapp.com",
  projectId: "你的项目ID",
  storageBucket: "你的项目.appspot.com",
  messagingSenderId: "你的-senderId",
  appId: "你的-appId"
};
```

### 5. 设置 Firestore 安全规则

1. 在 Firebase Console 中，点击 "Firestore Database"
2. 切换到 "规则" 标签
3. 将规则设置为（允许所有读写，适合个人使用）：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /bookings/{document=**} {
      allow read, write: if true;
    }
  }
}
```

4. 点击 "发布"

⚠️ **注意**：这个规则允许任何人读写数据。如果你需要更安全的设置，可以使用 Firebase Authentication。

### 6. 测试连接

1. 运行 `npm run dev` 启动开发服务器
2. 打开浏览器控制台
3. 应该看到 "✓ 成功获取 X 个预订" 或 "✓ 初始数据保存成功" 的日志

## ✅ 完成

配置完成后，所有数据都会自动保存到云端，并在所有设备间实时同步！

## 🔒 安全建议（可选）

如果你想让应用更安全，可以：

1. 启用 Firebase Authentication
2. 修改 Firestore 规则，只允许已登录用户访问
3. 使用环境变量存储 Firebase 配置（对于生产环境）

## 📝 注意事项

- 免费版 Firebase 有使用限制，但对于个人使用通常足够
- 数据会自动同步，无需手动刷新
- 如果网络断开，应用会显示错误信息

